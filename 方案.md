# fler — 完整方案文档（v3）

> Android 端 Flutter 逆向分析工具（纯原生实现）
> 基于 Kotlin + Material Design 3，集成 Blutter 引擎 + 单包分发 + SO 编辑器

---

## 一、项目概述

| 项目 | 说明 |
|------|------|
| 名称 | fler |
| 平台 | Android (原生) |
| 开发语言 | Kotlin + C++ (JNI) |
| UI 框架 | Jetpack Compose + Material Design 3 |
| 核心 | Blutter C++ 引擎 + 单包 7z 分发 + 动态链接共享库 + SO 编辑器 |
| 定位 | Flutter APK 逆向分析工具 |

### 核心功能

1. **Blutter 全功能分析** — 分析 APK/SO，直接写入 SQLite 数据库（pp_entries/classes/methods 表）
2. **单包引擎分发** — 首次启动下载 `fler-engines.7z`，包含所有 Dart 版本引擎 + 共享依赖（Capstone/ICU/c++_shared），后续零下载
3. **动态链接架构** — 引擎 so 动态链接共享库，单版本体积从 ~15MB 降至 ~2MB，12 版本总量 ~38MB（7z 压缩后 ~10-14MB）
4. **SO 浏览/编辑/修改** — Hex 编辑 + ELF 结构查看 + ARM64 指令补丁 + 撤销/备份
5. **自研 ELF 解析器** — <500KB，支持 ELF 结构查看 + 字节级读写
6. **ARM64 指令编码器** — 自研编码 ~50 条常用指令（可扩展架构），用于 SO 补丁
7. **产物↔SO 联动** — 地址翻译层自动关联 Dart VM 偏移 ↔ ELF 文件偏移
8. **国内加速** — Gitee 镜像 + GitHub 双源下载，国内用户提速 25 倍

### 整改要点（相对 v2 方案）

| 问题 | 整改措施 |
|------|----------|
| 引擎包体积过大（180MB/12版本） | 改为**动态链接共享库**（Capstone/ICU/c++_shared），单版本降至 ~2MB |
| 按需下载体验差 | 改为**单包 7z 分发**，首次下载 ~10-14MB 获得所有版本 |
| 国内下载慢（GitHub ~200KB/s） | 新增 **Gitee 镜像**，国内加速至 ~5MB/s |
| ICU 依赖未考虑 | 明确 ICU 为共享依赖，随引擎包分发 |
| Blutter 输出需要文件解析 | 改为**直接写 SQLite 数据库**，省去 pp/objs/asm 解析步骤 |
| 引擎下载管理分散 | 统一为 **EnginePackManager**，支持版本检测/升级/导入导出 |

---

## 二、目录结构

```
app/
├── src/
│   ├── main/
│   │   ├── java/com/fler/app/
│   │   │   ├── FlerApplication.kt              # Application 入口 + Hilt
│   │   │   ├── MainActivity.kt                  # 单 Activity 宿主
│   │   │   │
│   │   │   ├── app/
│   │   │   │   ├── navigation/
│   │   │   │   │   ├── AppNavGraph.kt            # NavHost 路由图
│   │   │   │   │   ├── Screen.kt                 # 路由 sealed class
│   │   │   │   │   └── BottomNavItem.kt          # 4 Tab 定义
│   │   │   │   └── theme/
│   │   │   │       ├── Theme.kt                  # Material 3 主题
│   │   │   │       ├── Color.kt                  # 配色方案
│   │   │   │       └── Type.kt                   # 字体排版
│   │   │   │
│   │   │   ├── core/
│   │   │   │   ├── jni/
│   │   │   │   │   ├── CapstoneBindings.kt       # Capstone JNI
│   │   │   │   │   ├── Arm64EncoderBindings.kt   # 编码器 JNI
│   │   │   │   │   ├── ElfParserBindings.kt     # 自研 ELF 解析器 JNI
│   │   │   │   │   └── BlutterBindings.kt        # Blutter 引擎 JNI
│   │   │   │   ├── network/
│   │   │   │   │   ├── GitHubClient.kt           # GitHub API + OkHttp
│   │   │   │   │   └── ProxyConfig.kt            # 代理配置模型
│   │   │   │   ├── db/
│   │   │   │   │   ├── AppDatabase.kt            # Room 数据库定义
│   │   │   │   │   ├── dao/
│   │   │   │   │   │   ├── ProjectDao.kt
│   │   │   │   │   │   ├── PpEntryDao.kt
│   │   │   │   │   │   ├── ObjInstanceDao.kt
│   │   │   │   │   │   ├── AsmFunctionDao.kt
│   │   │   │   │   │   ├── AddressMappingDao.kt
│   │   │   │   │   │   └── UndoRecordDao.kt
│   │   │   │   │   └── entity/
│   │   │   │   │       ├── ProjectEntity.kt
│   │   │   │   │       ├── PpEntryEntity.kt
│   │   │   │   │       ├── ObjInstanceEntity.kt
│   │   │   │   │       ├── AsmFunctionEntity.kt
│   │   │   │   │       ├── AddressMappingEntity.kt
│   │   │   │   │       └── UndoRecordEntity.kt
│   │   │   │   ├── model/
│   │   │   │   │   ├── Project.kt                # 项目模型
│   │   │   │   │   ├── ElfFile.kt                # ELF 文件模型
│   │   │   │   │   ├── ElfSection.kt             # ELF 节模型
│   │   │   │   │   ├── ElfSymbol.kt              # ELF 符号模型
│   │   │   │   │   ├── PpEntry.kt                # pp.txt 条目模型
│   │   │   │   │   ├── ObjInstance.kt            # objs.txt 实例模型
│   │   │   │   │   ├── AsmFunction.kt            # 反汇编函数模型
│   │   │   │   │   ├── AsmInstruction.kt         # 反汇编指令模型
│   │   │   │   │   └── AddressMapping.kt         # 地址映射模型
│   │   │   │   └── service/
│   │   │   │       ├── EnginePackManager.kt    # 引擎包管理（单包下载/解压/版本）
│   │   │   │       ├── SevenZipExtractor.kt    # 7z 解压工具
│   │   │   │       ├── BlutterEngine.kt        # Blutter 引擎 JNI 封装
│   │   │   │       ├── EngineLoader.kt         # 动态链接加载器（依赖链顺序加载）
│   │   │   │       ├── DualSourceDownloader.kt # Gitee/GitHub 双源下载
│   │   │   │       ├── AddressTranslator.kt    # 地址翻译层
│   │   │   │       ├── BackupManager.kt        # SO 备份/撤销管理
│   │   │   │       └── DartVersionDetector.kt  # Dart 版本检测
│   │   │   │
│   │   │   ├── features/
│   │   │   │   ├── project/
│   │   │   │   │   ├── ProjectListScreen.kt      # 项目列表
│   │   │   │   │   ├── ProjectDetailScreen.kt    # 项目详情
│   │   │   │   │   ├── ProjectNewDialog.kt       # 新建项目
│   │   │   │   │   └── ProjectViewModel.kt
│   │   │   │   ├── output/
│   │   │   │   │   ├── OutputHomeScreen.kt       # 产物主页
│   │   │   │   │   ├── PpBrowserScreen.kt        # pp.txt 列表
│   │   │   │   │   ├── PpDetailScreen.kt         # pp 条目详情
│   │   │   │   │   ├── ObjsBrowserScreen.kt      # objs.txt 列表
│   │   │   │   │   ├── ObjsDetailScreen.kt       # objs 实例详情
│   │   │   │   │   ├── AsmBrowserScreen.kt       # asm 文件树
│   │   │   │   │   ├── AsmViewerScreen.kt        # asm 内容查看
│   │   │   │   │   └── OutputViewModel.kt
│   │   │   │   ├── so_editor/
│   │   │   │   │   ├── SoEditorScreen.kt         # SO 编辑器主页
│   │   │   │   │   ├── ElfStructureTab.kt        # ELF 结构 Tab
│   │   │   │   │   ├── HexEditorTab.kt           # Hex 编辑 Tab
│   │   │   │   │   ├── DisassemblyTab.kt         # 反汇编 Tab
│   │   │   │   │   ├── ContextPanel.kt           # 地址上下文卡片
│   │   │   │   │   ├── EditInstructionSheet.kt   # 指令编辑 BottomSheet
│   │   │   │   │   └── SoEditorViewModel.kt
│   │   │   │   ├── engine/
│   │   │   │   │   ├── EngineDownloadScreen.kt   # 引擎下载进度页
│   │   │   │   │   ├── EngineManageScreen.kt     # 引擎管理页
│   │   │   │   │   └── EngineViewModel.kt
│   │   │   │   └── settings/
│   │   │   │       ├── SettingsScreen.kt         # 设置主页
│   │   │   │       ├── ProxySettingsScreen.kt    # 代理设置
│   │   │   │       └── SettingsViewModel.kt
│   │   │   │
│   │   │   └── ui/
│   │   │       └── components/
│   │   │           ├── CardListTile.kt           # 卡片列表项
│   │   │           ├── HexView.kt                # Hex 渲染
│   │   │           ├── TreeView.kt               # 树形组件
│   │   │           ├── InfoCard.kt               # 上下文信息卡片
│   │   │           ├── CodeHighlight.kt          # 代码着色组件
│   │   │           ├── ShimmerPlaceholder.kt     # 骨架屏组件
│   │   │           ├── EmptyState.kt             # 空状态提示
│   │   │           └── ErrorState.kt             # 错误状态提示
│   │   │
│   │   ├── assets/
│   │   │   ├── tutorial/                         # 新手引导资源
│   │   │   └── demo/                             # 预置演示数据
│   │   │       ├── sample_analysis/
│   │   │       └── sample.so
│   │   │
│   │   ├── jniLibs/arm64-v8a/                   # 原生 .so（预编译）
│   │   │   ├── libcapstone.so
│   │   │   ├── libarm64_encoder.so
│   │   │   └── libelf_parser.so                 # 自研 ELF 解析器
│   │   │
│   │   ├── cpp/
│   │   │   ├── CMakeLists.txt                    # NDK 构建入口
│   │   │   └── jni_bridge/                       # JNI 桥接层
│   │   │       ├── capstone_jni.cpp
│   │   │       ├── arm64_encoder_jni.cpp
│   │   │       ├── elf_parser_jni.cpp
│   │   │       └── blutter_jni.cpp
│   │   │
│   │   └── res/
│   │       ├── values/                           # Material 3 主题属性
│   │       └── drawable/
│   │
│   ├── test/                                     # 单元测试
│   │   ├── core/service/
│   │   │   ├── EnginePackManagerTest.kt
│   │   │   ├── EngineLoaderTest.kt
│   │   │   ├── DualSourceDownloaderTest.kt
│   │   │   ├── SevenZipExtractorTest.kt
│   │   │   ├── AddressTranslatorTest.kt
│   │   │   ├── BackupManagerTest.kt
│   │   │   └── DartVersionDetectorTest.kt
│   │   └── features/
│   │       └── ProjectViewModelTest.kt
│   │
│   └── androidTest/                              # 集成测试
│       ├── BlutterEngineIntegrationTest.kt
│       └── SoEditorIntegrationTest.kt
│
├── native/                                       # C++ 原生源码
│   ├── arm64_encoder/                            # 轻量 ARM64 编码器
│   │   ├── include/
│   │   │   ├── arm64_encoder.h
│   │   │   └── instruction_registry.h            # 指令注册接口
│   │   ├── src/
│   │   │   ├── encoder.cpp
│   │   │   ├── registry.cpp
│   │   │   ├── dp.cpp                            # 数据处理指令
│   │   │   ├── load_store.cpp                    # 加载存储指令
│   │   │   └── branch.cpp                        # 分支指令
│   │   └── tests/
│   │       └── encoder_test.cpp
│   ├── elf_parser/                               # 自研轻量 ELF 解析器（替代 LIEF）
│   │   ├── CMakeLists.txt
│   │   ├── include/
│   │   │   ├── elf_parser.h                      # 公共 API
│   │   │   ├── elf_section.h                     # 节头解析
│   │   │   ├── elf_symbol.h                     # 符号表解析
│   │   │   └── elf_writer.h                      # 字节级写入
│   │   └── src/
│   │   │   ├── elf_parser.cpp
│   │   │   ├── elf_section.cpp
│   │   │   ├── elf_symbol.cpp
│   │   │   └── elf_writer.cpp
│   │   └── tests/
│   │       └── elf_parser_test.cpp
│   └── capstone/                                 # Capstone 反汇编
│       └── CMakeLists.txt
│
├── build.gradle.kts                              # 模块级构建
├── proguard-rules.pro
└── lint.xml
```

---

## 三、导航设计

### 3.1 底部导航规则

```
┌──────────┬──────────┬──────────┬──────────┐
│  📁 项目  │  📦 产物  │  🔧 SO   │  ⚙️ 设置 │
│ Project  │  Output  │  Editor  │ Settings │
└──────────┴──────────┴──────────┴──────────┘
```

**关键规则：**
- 底部导航**仅**在顶层 4 个页面显示
- 进入任何子页面时，**隐藏底部导航栏**，改用 AppBar 返回按钮
- 通过 `NavigationBar` + `NavHost` 的路由动画实现平滑切换

### 3.2 路由设计

| 路由 | 页面 | BottomNav | 说明 |
|------|------|-----------|------|
| `/` | 项目列表 | ✅ 显示 | 卡片列表 + 统计摘要 + Shimmer 加载 |
| `/project/new` | 新建项目 | ❌ 隐藏 | 选文件（SAF）+ 设置名称 + 架构检测 |
| `/project/:id` | 项目详情 | ❌ 隐藏 | 概览 + SO 文件列表 + 操作入口 |
| `/project/:id/download` | 下载引擎 | ❌ 隐藏 | 引擎下载进度 + 日志显示 |
| `/output/:id` | 产物主页 | ❌ 隐藏 | 分析摘要 + pp/objs/asm 卡片入口 |
| `/output/:id/pp` | pp.txt 列表 | ❌ 隐藏 | 偏移/类型/摘要卡片列表 + 搜索筛选 |
| `/output/:id/pp/:off` | pp 条目详情 | ❌ 隐藏 | 对象完整描述 + 交叉引用 + 地址翻译 |
| `/output/:id/objs` | objs.txt 列表 | ❌ 隐藏 | 实例卡片列表 + 搜索 |
| `/output/:id/objs/:addr` | objs 实例详情 | ❌ 隐藏 | 字段树形展开 + 交叉引用 |
| `/output/:id/asm` | asm 文件树 | ❌ 隐藏 | 按库/类组织的文件树 + 函数搜索 |
| `/output/:id/asm/:file` | asm 内容 | ❌ 隐藏 | 反汇编显示 + 函数标签切换 |
| `/so/:id` | SO 文件选择 | ❌ 隐藏 | 项目内 SO 文件列表卡片 |
| `/so/:id/:sofile` | SO 编辑器 | ❌ 隐藏 | 3个滑动 Tab + 撤销/备份 |
| `/settings` | 设置主页 | ✅ 显示 | 代理/引擎/缓存卡片 |
| `/settings/proxy` | 代理设置 | ❌ 隐藏 | HTTP/SOCKS5 配置 + 测试 |
| `/settings/engine` | 引擎管理 | ❌ 隐藏 | 缓存引擎列表 + 版本统计 |
| `/tutorial` | 新手引导 | ❌ 隐藏 | 首次使用交互式向导 |

---

## 四、版本兼容性矩阵

### 4.1 已支持的 Dart 版本

| Dart 版本 | 引擎 so 名称 | 状态 | 单版本大小 |
|-----------|-------------|------|-----------|
| 3.13.0 | dartvm_3.13.0_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.12.2 | dartvm_3.12.2_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.12.1 | dartvm_3.12.1_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.12.0 | dartvm_3.12.0_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.11.5 | dartvm_3.11.5_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.11.4 | dartvm_3.11.4_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.10.7 | dartvm_3.10.7_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.10.6 | dartvm_3.10.6_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.9.2 | dartvm_3.9.2_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.9.1 | dartvm_3.9.1_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.8.1 | dartvm_3.8.1_android_arm64.so | ✅ 已编译 | ~2.2MB |
| 3.8.0 | dartvm_3.8.0_android_arm64.so | ✅ 已编译 | ~2.2MB |

### 4.2 共享依赖库（所有版本共用，仅下载一次）

| 库 | 大小 | 说明 |
|----|------|------|
| libc++_shared.so | ~1.5MB | NDK C++ 标准库（动态链接） |
| libcapstone_arm64-v8a.so | ~2MB | Capstone ARM64 反汇编引擎（动态链接） |
| libicuuc.so | ~3MB | ICU Unicode 功能库（DartVM 依赖） |
| libicudata.so | ~5MB | ICU 数据表（DartVM 依赖） |

### 4.3 引擎包结构（单包分发）

```
fler-engines.7z  (7z 压缩后 ~10-14MB)
│
├── shared/                              ← 共享依赖（所有版本共用）
│   ├── libc++_shared.so              ~1.5MB
│   ├── libcapstone_arm64-v8a.so      ~2MB
│   ├── libicuuc.so                   ~3MB
│   └── libicudata.so                 ~5MB
│
├── dartvm_3.13.0_android_arm64.so   ~2.2MB
├── dartvm_3.12.2_android_arm64.so   ~2.2MB
├── dartvm_3.12.1_android_arm64.so   ~2.2MB
├── dartvm_3.12.0_android_arm64.so   ~2.2MB
├── dartvm_3.11.5_android_arm64.so   ~2.2MB
├── dartvm_3.11.4_android_arm64.so   ~2.2MB
├── dartvm_3.10.7_android_arm64.so   ~2.2MB
├── dartvm_3.10.6_android_arm64.so   ~2.2MB
├── dartvm_3.9.2_android_arm64.so    ~2.2MB
├── dartvm_3.9.1_android_arm64.so    ~2.2MB
├── dartvm_3.8.1_android_arm64.so    ~2.2MB
└── dartvm_3.8.0_android_arm64.so    ~2.2MB

原始大小: ~38MB → 7z 压缩后: ~10-14MB
```

### 4.4 动态链接架构说明

**之前的问题（静态链接）：**
```
dartvm_3.12.2.so = Blutter(3MB) + DartVM(8MB) + Capstone(2MB) + c++_static(2MB) + SQLite(1MB)
dartvm_3.12.1.so = Blutter(3MB) + DartVM(8MB) + Capstone(2MB) + c++_static(2MB) + SQLite(1MB)
... 每个版本都重复包含 Capstone + c++_static + ICU → 12 版本共浪费 ~144MB
```

**优化后（动态链接）：**
```
shared/libcapstone_arm64-v8a.so  ← Capstone 只存 1 份
shared/libc++_shared.so          ← STL 只存 1 份
shared/libicuuc.so               ← ICU 只存 1 份
shared/libicudata.so             ← ICU 数据只存 1 份

dartvm_3.12.2.so = Blutter核心(3MB) + DartVM 3.12.1静态(8MB) + SQLite(1MB) → 依赖动态加载共享库
dartvm_3.12.1.so = Blutter核心(3MB) + DartVM 3.12.2静态(8MB) + SQLite(1MB) → 依赖动态加载共享库
... 每个版本仅包含版本相关的 DartVM 静态库，共享依赖只存 1 份
```

### 4.5 引擎加载顺序（依赖链）

```kotlin
// 必须严格按依赖顺序加载
val loadOrder = listOf(
    "libc++_shared.so",            // C++ STL（最底层依赖）
    "libicuuc.so",                 // ICU 功能库
    "libicudata.so",               // ICU 数据表
    "libcapstone_arm64-v8a.so",    // 反汇编引擎
    "dartvm_3.12.2_android_arm64.so"  // 具体版本引擎（依赖以上所有）
)

fun loadAll(dartVersion: String) {
    for (lib in loadOrder) {
        val file = File(engineDir, lib)
        if (file.exists()) System.load(file.absolutePath)
    }
}
```

### 4.6 下载源

| 源 | URL | 说明 |
|----|-----|------|
| **Gitee（国内加速）** | https://gitee.com/fler/fler-dart/releases | 国内下载 ~5MB/s |
| **GitHub（全球）** | https://github.com/fler/fler-dart/releases | 国外下载 ~500KB/s |

### 版本检测逻辑

```kotlin
fun detectDartVersion(apkFile: File): String? {
    // Step 1: 解压 APK，提取 lib/arm64-v8a/libflutter.so
    val libflutterSo = extractSoFromApk(apkFile, "libflutter.so")
    
    // Step 2: 使用自研 ElfParser 解析 .rodata 段
    val elfParser = ElfParser()
    elfParser.open(libflutterSo.absolutePath)
    val rodataSection = elfParser.getSection(".rodata")
    val rodataData = elfParser.getSectionData(".rodata")
    
    // Step 3: 在 .rodata 中搜索 Dart 版本字符串
    // Dart 版本存储格式: "Dart SDK version: 3.12.2" 或版本符号
    val versionRegex = Regex("""Dart SDK version:\s*(\d+\.\d+\.\d+)""")
    val versionString = rodataData?.let { data ->
        versionRegex.find(String(data))?.groupValues?.get(1)
    }
    
    // Step 4: 如果 .rodata 找不到，尝试从符号表查找
    if (versionString == null) {
        val symbols = elfParser.getSymbols()
        // Dart VM 版本相关符号: DartVersion, dart_version, version_
        val versionSymbol = symbols.find { it.name.contains("ersion", ignoreCase = true) }
        // 从符号地址读取版本字符串
    }
    
    // Step 5: 匹配兼容版本列表
    val supportedVersions = listOf("3.13.0", "3.12.2", "3.12.1", "3.12.0", "3.11.5", "3.11.4", "3.10.7", "3.10.6", "3.9.2", "3.9.1", "3.8.1", "3.8.0")
    return versionString?.takeIf { it in supportedVersions }
}
```

**备选方案（如果 libflutter.so 不含版本字符串）：**
- 分析 DartVM 初始化函数的硬编码版本号
- 从 Dart VM 快照格式特征匹配已知版本
- 使用 `strings libflutter.so | grep "Dart"` 命令行方式

---

## 五、引擎包下载与管理架构

### 5.1 EnginePackManager 设计

```kotlin
@Singleton
class EnginePackManager @Inject constructor(
    private val context: Context,
    private val giteeApi: GiteeApi,
) {
    private val engineDir: File by lazy {
        File(context.filesDir, "engines").apply { mkdirs() }
    }

    /** 首次启动 / 版本升级时下载引擎包 */
    suspend fun ensureEnginesReady(): Flow<EngineProgress>

    /** 加载指定 Dart 版本的引擎（自动检查共享依赖） */
    fun loadEngine(dartVersion: String): BlutterEngine

    /** 检查引擎包版本更新 */
    suspend fun checkForUpdates(): EngineUpdate?

    /** 导出引擎包到本地文件（用户备份） */
    suspend fun exportEnginePack(target: File)

    /** 从本地文件导入引擎包 */
    suspend fun importEnginePack(source: File)

    /** 列出所有已安装的引擎版本 */
    fun listInstalledEngines(): List<InstalledEngine>
}

sealed class EngineProgress {
    data class Downloading(val pct: Float, val downloadedMB: Long, val totalMB: Long, val speed: String) : EngineProgress()
    data class Extracting(val pct: Float) : EngineProgress()
    data class Verifying(val pct: Float) : EngineProgress()
    data object Ready : EngineProgress()
    data class Failed(val error: String) : EngineProgress()
}

data class InstalledEngine(
    val dartVersion: String,
    val enginePath: File,
    val sizeMB: Long,
    val installedAt: Long,
)

data class EngineUpdate(
    val newVersion: String,
    val changelog: String,
    val sizeMB: Long,
)
```

### 5.2 引擎包下载流程

```
首次启动 fler / 引擎包升级时:
        │
        ▼
检查本地引擎包版本
  ├── 最新版本 → 直接可用 → 跳过下载
  └── 无引擎包 / 需要升级 → 显示下载页
                │
                ▼
            用户确认 → 开始下载 fler-engines.7z
                │
                ▼
            ┌─── 并行下载策略 ───┐
            │ 1. 优先 Gitee（国内）│
            │ 2. 失败回退 GitHub   │
            │ 3. 支持断点续传     │
            └─────────────────────┘
                │
                ▼
            显示下载进度 + 速度 + 预估剩余
                │
                ▼
            下载完成 → SHA256 校验
                │
                ▼
            7z 解压到 filesDir/engines/
                │
                ▼
            验证所有文件就位
                │
                ▼
            引擎就绪 → 后续分析任意版本零下载
```

### 5.3 7z 解压实现

```kotlin
object SevenZipExtractor {
    /** 解压 7z 文件到目标目录 */
    suspend fun extract(archive: File, targetDir: File, onProgress: (Float) -> Unit) {
        withContext(Dispatchers.IO) {
            targetDir.mkdirs()
            
            // 使用内置的 7z 工具（打包在 assets 或 JNI）
            val process = ProcessBuilder(listOf(
                SevenZipPaths.SEVEN_ZIP_BINARY,
                "x", archive.absolutePath,
                "-o${targetDir.absolutePath}",
                "-y",  // 覆盖已有文件
                "-mx=0"  // 无压缩（解压时不需要）
            )).start()
            
            // 读取进度输出
            val reader = BufferedReader(InputStreamReader(process.inputStream))
            var line: String?
            while (reader.readLine().also { line = it } != null) {
                line?.let { parseAndReportProgress(it, onProgress) }
            }
            
            process.waitFor()
        }
    }
}
```

### 5.4 动态链接加载策略

```kotlin
class EngineLoader @Inject constructor() {
    private val loadedLibs = mutableSetOf<String>()
    private val loadLock = Any()

    /** 确保所有共享依赖已加载（进程内只加载一次） */
    fun ensureSharedLibsLoaded() {
        synchronized(loadLock) {
            val sharedLibs = listOf(
                "libc++_shared.so",
                "libicuuc.so",
                "libicudata.so",
                "libcapstone_arm64-v8a.so",
            )
            for (libName in sharedLibs) {
                if (libName !in loadedLibs) {
                    val libFile = File(engineDir, libName)
                    if (libFile.exists()) {
                        System.load(libFile.absolutePath)
                        loadedLibs.add(libName)
                    }
                }
            }
        }
    }

    /** 加载指定 Dart 版本的引擎 */
    fun loadEngine(dartVersion: String): BlutterEngine {
        ensureSharedLibsLoaded()
        
        val engineName = "dartvm_${dartVersion}_android_arm64.so"
        val engineFile = File(engineDir, engineName)
        
        if (!engineFile.exists()) {
            throw EngineNotReadyException(dartVersion)
        }
        
        synchronized(loadLock) {
            if (engineName !in loadedLibs) {
                System.load(engineFile.absolutePath)
                loadedLibs.add(engineName)
            }
        }
        
        return BlutterEngine(dartVersion)
    }
}

class BlutterEngine(private val version: String) {
    private var created = false

    /** 创建 Blutter 分析引擎实例 */
    fun create(): Long {
        // 调用 JNI: nativeCreate(version, logCb, progressCb)
        val ptr = nativeCreate(version, null, null)
        if (ptr == 0L) throw EngineCreateException(version)
        created = true
        return ptr
    }

    /** 运行完整分析 */
    fun analyze(ptr: Long, soPath: String, dbPath: String): Int {
        return nativeBlutterAnalyze(ptr, soPath, dbPath)
    }

    /** 销毁引擎 */
    fun destroy(ptr: Long) {
        if (created) {
            nativeDestroy(ptr)
            created = false
        }
    }

    // Native 方法声明（对应 JNI 桥接层）
    private external fun nativeCreate(version: String, logCb: ((Int, String) -> Unit)?, progressCb: ((Float, String) -> Unit)?): Long
    private external fun nativeBlutterAnalyze(ptr: Long, soPath: String, dbPath: String): Int
    private external fun nativeDestroy(ptr: Long)
}
```

### 5.5 前台服务设计

```kotlin
class EngineDownloadService : Service() {
    // 通知通道: "引擎包下载"
    // 通知 ID: 1001

    fun startDownload() {
        startForeground(NOTIFICATION_ID, buildNotification(0f, "初始化下载..."))
        // 在协程中执行下载 + 解压流程
    }

    fun updateProgress(progress: Float, stage: String, speed: String = "") {
        val notification = buildNotification(progress, stage, speed)
        notificationManager.notify(NOTIFICATION_ID, notification)
    }

    fun stopService()
}
```

**Android 14+ 合规要点：**
- 使用 `FOREGROUND_SERVICE_TYPE_DATA_SYNC` 类型
- 调用 `startForeground()` 必须在 `startService()` 后 5 秒内
- 提供持久通知，不可滑动取消
- 处理用户关闭通知的情况（显示继续下载提示）

### 5.6 缓存管理策略

| 项目 | 保留策略 | 清理方式 |
|------|----------|----------|
| 引擎包（所有版本） | 永久，直到手动删除或升级 | 设置页手动管理 |
| 下载临时文件 | 解压完成后自动清理 | 自动 |
| 解压临时文件 | 解压完成后自动清理 | 自动 |
| 数据库缓存 | 项目删除时级联清理 | 自动 |
| 分析产物 | 项目删除时级联清理 | 自动 |

### 5.7 Gitee/GitHub 双源策略

```kotlin
class DualSourceDownloader @Inject constructor(
    private val giteeApi: GiteeApi,
    private val githubApi: GitHubApi,
) {
    /** 下载引擎包，优先 Gitee，失败回退 GitHub */
    suspend fun downloadEnginePack(
        onProgress: (downloaded: Long, total: Long, speed: String) -> Unit
    ): File {
        val target = File(context.cacheDir, "fler-engines.7z")

        return try {
            // 优先 Gitee（国内加速）
            giteeApi.downloadEnginePack(target, onProgress)
        } catch (e: Exception) {
            // Gitee 失败，回退 GitHub
            githubApi.downloadEnginePack(target, onProgress)
        }
    }
}
```

---

## 六、自研 ELF 解析器设计（替代 LIEF）

### 6.1 设计目标

- **体积**：<500KB（编译后 stripped）
- **依赖**：零外部依赖，仅标准 C++ 库
- **功能**：ELF 结构解析 + 字节级读写 + 符号表查询
- **架构**：ARM64 原生编译，`jniLibs/` 预打包

### 6.2 核心 API

```kotlin
// ElfParserBindings.kt (Kotlin 层)
interface ElfParser {
    /** 打开 ELF 文件 */
    fun open(path: String): Long  // 返回 native handle
    
    /** 关闭 ELF 文件 */
    fun close(handle: Long)
    
    // === 只读 API ===
    
    /** 获取 ELF 头信息 */
    fun getHeader(handle: Long): ElfHeader
    
    /** 列出所有节头 */
    fun getSections(handle: Long): List<ElfSection>
    
    /** 获取指定节的原始数据 */
    fun getSectionData(handle: Long, sectionName: String): ByteArray?
    
    /** 列出所有符号（.symtab + .dynsym） */
    fun getSymbols(handle: Long): List<ElfSymbol>
    
    /** 根据名称查找符号地址 */
    fun findSymbolAddress(handle: Long, symbolName: String): Long?
    
    /** 读取指定偏移处的字节 */
    fun readBytes(handle: Long, offset: Long, size: Int): ByteArray
    
    // === 写入 API ===
    
    /** 写入字节到指定偏移 */
    fun writeBytes(handle: Long, offset: Long, data: ByteArray): Boolean
    
    /** 将修改持久化到文件 */
    fun flush(handle: Long): Boolean
    
    /** 校验指定偏移处的 CRC32 */
    fun computeCRC32(handle: Long, offset: Long, size: Int): UInt
}

data class ElfHeader(
    val magic: String,           // "\x7fELF"
    val architecture: String,    // "ARM aarch64"
    val endianness: String,      // "Little endian"
    val version: Int,
    val entryPoint: Long,
    val sectionHeaderOffset: Long,
    val sectionHeaderCount: Int,
    val sectionHeaderSize: Int
)

data class ElfSection(
    val name: String,
    val type: String,            // "SHT_PROGBITS", "SHT_SYMTAB" 等
    val offset: Long,            // 文件偏移
    val size: Long,
    val address: Long,           // 虚拟地址
    val flags: String
)

data class ElfSymbol(
    val name: String,
    val address: Long,           // 虚拟地址
    val size: Long,
    val type: String,            // "STT_FUNC", "STT_OBJECT" 等
    val binding: String,         // "STB_GLOBAL", "STB_LOCAL" 等
    val sectionIndex: Int
)
```

### 6.3 C++ 实现要点

```cpp
// elf_parser.h
class ElfParser {
public:
    // 文件操作
    static ElfParser* open(const char* path);
    ~ElfParser();
    
    // 只读
    ElfHeader getHeader() const;
    std::vector<ElfSection> getSections() const;
    std::vector<uint8_t> getSectionData(const char* sectionName) const;
    std::vector<ElfSymbol> getSymbols() const;
    uint64_t findSymbolAddress(const char* name) const;
    std::vector<uint8_t> readBytes(uint64_t offset, size_t size) const;
    
    // 写入
    bool writeBytes(uint64_t offset, const uint8_t* data, size_t size);
    bool flush();
    uint32_t computeCRC32(uint64_t offset, size_t size) const;
    
private:
    int fd_;
    Elf64_Ehdr header_;
    std::vector<Elf64_Shdr> sectionHeaders_;
    
    void parseSectionHeaders();
    void parseSymbolTable(int sectionIndex);
};
```

**实现策略：**
- 使用 `mmap` 映射文件（ARM64 支持），提高读取效率
- 只读操作全部基于内存映射
- 写入操作使用 `pwrite` 系统调用
- 符号表解析支持 `.symtab` 和 `.dynsym`
- 字符串表解析支持 `.strtab` 和 `.dynstr`

### 6.4 需要的 ELF 结构

| 结构 | 读取 | 写入 | 用途 |
|------|------|------|------|
| ELF 头 | ✅ | ❌ | 展示架构信息 |
| 节头表 | ✅ | ❌ | 展示节列表、定位 .text 节 |
| 程序头 | ✅ | ❌ | 段映射、内存权限 |
| .symtab | ✅ | ❌ | 查找函数符号地址 |
| .strtab | ✅ | ❌ | 符号名解析 |
| .dynsym | ✅ | ❌ | 动态符号查找 |
| .dynstr | ✅ | ❌ | 动态符号名 |
| .text 段数据 | ✅ | ✅ | 指令补丁的目标区域 |
| .rodata 段数据 | ✅ | ❌ | 只读数据查看 |

---

## 七、AddressTranslator 设计（基于实际 Blutter 输出格式）

### 7.1 Blutter 输出格式分析

基于 `E:\111\bitcontrol\out` 实际数据分析：

**pp.txt 格式：**
```
pool heap offset: 0xa00080           ← Dart VM 堆基址
[pp+0x10] Stub: Subtype7TestCache (0x3324e0)  ← Stub 直接带 ELF 地址
[pp+0x40] List(5) [0x1, 0x2, 0x2, 0x2, Null]  ← 列表
[pp+0x50] String: "file:///..."     ← 字符串
[pp+0x88] Field <UrlLauncherPlatform._token@577332722>: static late final (offset: 0xa8c)  ← 字段
[pp+0x98] Obj!Object@8690e1          ← 对象（带 Dart 堆偏移）
```

**objs.txt 格式：**
```
Obj!Uuid@8bb111 : {                  ← 对象实例，Dart 堆偏移 0x8bb111
}
Obj!FlutterAuthClientOptions@8bb141 : {
  Super!AuthClientOptions : {
    off_8: true,
    off_10_Obj!AuthFlowType@8d24d1 : {
      Super!_Enum : {
        off_8: int(0x1),
        off_10: "pkce"
      }
    }
  },
  off_18: true
}
```

**asm/ 格式：**
```
// lib: , url: package:bitmanager/main.dart
// class id: 1048608, size: 0x8
class :: {
  static void main() async {
    // ** addr: 0x829ba0, size: 0x90      ← ELF 地址
    // 0x829ba0: EnterFrame
    //     0x829ba0: stp             fp, lr, [SP, #-0x10]!
    //     0x829ba4: mov             fp, SP
    // 0x829ba8: AllocStack(0x20)
    //     0x829ba8: sub             SP, SP, #0x20
    // ...
    //     0x829bbc: ldr             x0, [PP, #0x428]  ; [pp+0x428] TypeArguments: <void?>
    //     0x829bc0: bl              #0x34edd8  ; InitAsyncStub
    // ...
    //     0x829bc4: bl              #0x3a5ce8  ; [package:flutter/src/widgets/binding.dart] WidgetsFlutterBinding::ensureInitialized
  }
}
```

### 7.2 具体映射算法

```
映射关系:
  Dart VM 堆偏移 (pp+0xXX)  ←→  ELF 虚拟地址 (0xYYYY)
  
  数据来源:
  1. Stub 条目 (pp.txt): 直接包含 ELF 地址
     [pp+0x10] Stub: Subtype7TestCache (0x3324e0)
     → Dart VM 偏移 = pool_heap_base + 0x10
     → ELF 地址 = 0x3324e0
     
  2. asm 交叉引用 (asm/ 文件): 每行包含两种地址
     // 0x829bbc: ldr x0, [PP, #0x428]  ; [pp+0x428] TypeArguments: <void?>
     → ELF 地址 = 0x829bbc
     → Dart VM 偏移 = pool_heap_base + 0x428
     
  3. 函数入口 (asm/ 文件):
     // ** addr: 0x829ba0, size: 0x90
     → 函数 ELF 地址 = 0x829ba0
     → 可通过符号表查找对应函数名
```

### 7.3 AddressTranslator 实现

```kotlin
class AddressTranslator @Inject constructor(
    private val project: Project,
    private val addressMappingDao: AddressMappingDao,
    private val elfParser: ElfParser,
) {
    private var poolHeapBase: Long = 0
    private val mappingCache = mutableMapOf<String, AddressMapping>()

    /**
     * 初始化：解析 pp.txt 首行获取 pool heap base，构建映射表
     */
    suspend fun initialize(ppFile: File, asmDir: File) {
        // 1. 解析 pp.txt 第一行: "pool heap offset: 0xa00080"
        poolHeapBase = parsePoolHeapBase(ppFile)
        
        // 2. 解析 pp.txt 中的 Stub 条目，直接提取 ELF 地址
        parseStubMappings(ppFile)
        
        // 3. 解析 asm/ 下所有 .dart 文件，提取 [pp+0xXX] ↔ 0xYYYY 映射
        parseAsmMappings(asmDir)
        
        // 4. 批量写入 Room
        addressMappingDao.insertAll(mappingCache.values.toList())
    }
    
    /**
     * 将 Dart VM 堆偏移转换为 ELF 文件偏移
     * @param vmOffset Dart VM 堆偏移 (绝对地址)
     * @param soName SO 文件名
     * @return ELF 文件偏移 (可能为 null 表示未找到)
     */
    suspend fun vmOffsetToFileOffset(vmOffset: Long, soName: String): Long? {
        // 1. 查询缓存
        val key = "${soName}_${vmOffset.toString(16)}"
        mappingCache[key]?.let { return it.fileOffset }
        
        // 2. 查询数据库
        val mapping = addressMappingDao.findByVmOffset(vmOffset, soName)
        mapping?.let {
            mappingCache[key] = it
            return it.fileOffset
        }
        
        return null
    }
    
    /**
     * 获取地址的上下文信息
     */
    suspend fun getContext(address: Long, soName: String): AddressContext {
        return AddressContext(
            address = address,
            function = findFunctionByAddress(address),
            ppEntries = findPpReferences(address),
            objInstances = findObjReferences(address),
            disassembly = disassembleAt(address)
        )
    }
    
    // === 私有方法 ===
    
    private fun parsePoolHeapBase(ppFile: File): Long {
        // 读取第一行: "pool heap offset: 0xa00080"
        // 提取 0xa00080 部分
    }
    
    private suspend fun parseStubMappings(ppFile: File) {
        // 逐行解析: [pp+0xXX] Stub: Name (0xYYYY)
        // Dart VM 偏移 = poolHeapBase + 0xXX
        // ELF 地址 = 0xYYYY
        // 构建映射表
    }
    
    private suspend fun parseAsmMappings(asmDir: File) {
        // 遍历 asm/ 下所有 .dart 文件
        // 逐行查找: // 0xYYYY: ... ; [pp+0xXX] ...
        // 每行包含 ELF 地址 (0xYYYY) 和 Dart VM 偏移 (poolHeapBase + 0xXX)
        // 构建双向映射
    }
}

data class AddressMapping(
    val vmOffset: Long,       // Dart VM 绝对堆偏移
    val fileOffset: Long,     // ELF 文件偏移
    val elfAddress: Long,     // ELF 虚拟地址
    val section: String,      // 所在节名称 (如 ".text")
    val symbol: String?,      // 关联的函数/符号名
    val soName: String,       // 所属 SO 文件名
)

data class AddressContext(
    val address: Long,
    val function: AsmFunction?,
    val ppEntries: List<PpEntry>,
    val objInstances: List<ObjInstance>,
    val disassembly: String?,
)
```

### 7.4 翻译流程图

```
用户在产物页点击 [SO中定位]
        │
        ▼
vmOffsetToFileOffset(vmOffset, soName)
        │
        ├── 内存缓存命中 → 直接跳转 Hex 编辑器
        │
        ├── Room 缓存命中 → 返回结果 + 更新缓存
        │
        └── 缓存未命中 → 遍历 pp.txt + asm/ 文件
                    → 搜索匹配的映射关系
                    → 计算 ELF 文件偏移
                    → 缓存到内存 + Room
                    → 返回
        │
        ▼
SO 编辑器自动:
  1. 切换到正确的 SO 文件
  2. 跳转到对应偏移位置
  3. 显示 ContextPanel 上下文
  4. 可选切换到反汇编 Tab
```

---

## 八、SO 编辑安全机制

### 8.1 BackupManager（Room 持久化版）

```kotlin
class BackupManager @Inject constructor(
    private val undoRecordDao: UndoRecordDao,
    private val soDir: File,
) {
    private val undoStack = ArrayDeque<PatchRecord>()  // 会话级内存缓存
    private val MAX_UNDO = 50

    /** 首次编辑前调用：创建 .bak 全量备份 */
    suspend fun createBackupIfNeeded(soFile: File)

    /** 应用补丁前调用：记录字节级 diff 到 Room */
    suspend fun recordPatch(
        address: Long,
        oldBytes: ByteArray,
        newBytes: ByteArray,
        soName: String,
    )

    /** 撤销最近一次补丁（优先内存栈，降级到 Room） */
    suspend fun undo(soName: String): Boolean

    /** 从 .bak 完整恢复 */
    suspend fun restoreFromBackup(soFile: File)

    /** 导出补丁为 .patch 文件 */
    suspend fun exportPatch(soFile: File, outputPath: String)

    /** 获取历史撤销记录（跨会话） */
    suspend fun getHistory(soName: String): List<UndoRecord>

    /** 清空历史记录 */
    suspend fun clearHistory(soName: String)
}

@Entity
data class UndoRecordEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val soName: String,
    val address: Long,
    val oldBytes: ByteArray,
    val newBytes: ByteArray,
    val timestamp: Long,
)
```

### 8.2 安全流程

```
用户点击 [应用补丁]
        │
        ▼
Step 1: 备份检查
  ├── 是否有 .bak？→ 没有则创建全量备份
  └── 有则跳过
        │
        ▼
Step 2: CRC 校验
  ├── 计算当前地址的原字节 CRC32
  ├── 与编辑时记录的原始字节对比
  ├── ✅ 匹配 → 继续
  └── ❌ 不匹配 → 提示"SO 已变化，请重新分析"
        │
        ▼
Step 3: 写入补丁
  ├── 写入新的字节
  ├── 验证写入后的 CRC
  ├── ✅ 通过 → 记录到撤销栈 + Room
  └── ❌ 失败 → 从 .bak 恢复 + 报错
        │
        ▼
Step 4: UI 刷新
  ├── 刷新 Hex 视图
  ├── 刷新反汇编视图
  └── 更新撤销按钮状态
```

---

## 九、页面 UI 设计

### 9.1 项目列表页 `/`

```
┌──────────────────────────────┐
│  [AppBar] fler               │
├──────────────────────────────┤
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 📱 示例App         ││
│  │  Dart 3.12.2              ││
│  │  ┌─────┬──────┬─────┐   ││
│  │  │ 284 │ 1203 │  42 │   ││
│  │  │ 类   │ 函数  │ Stub│   ││
│  │  └─────┴──────┴─────┘   ││
│  │  📅 2025-07-30        ▶ ││
│  └──────────────────────────┘│
│                              │
│  FAB: [+ 新建项目]           │
│                              │
├──────────────────────────────┤
│  NavigationBar               │
│  📁  📦  🔧  ⚙️             │
└──────────────────────────────┘
```

### 9.2 项目详情页 `/project/:id`

```
┌──────────────────────────────┐
│  ← [AppBar] 示例App          │
├──────────────────────────────┤
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 📊 分析概览        ││
│  │  ┌─────┬──────┬─────┐   ││
│  │  │ 284 │ 1203 │  42 │   ││
│  │  │ 类   │ 函数  │ Stub│   ││
│  │  └─────┴──────┴─────┘   ││
│  │  FilledButton: ▶ 运行    ││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 🔧 引擎状态        ││
│  │  Dart 3.12.2 ✅ 已就绪   ││
│  │  [重新下载 ▶]             ││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 📦 SO 文件         ││
│  │  libapp.so     4.2MB  ▶ ││
│  │  libflutter.so 12MB   ▶ ││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 🗂 分析输出         ││
│  │  状态: ✅ 已分析           ││
│  │  [📂 查看产物 ▶]          ││
│  └──────────────────────────┘│
│                              │
└──────────────────────────────┘
```

### 9.3 引擎下载页 `/project/:id/download`

```
┌──────────────────────────────┐
│  ← [AppBar] 下载引擎         │
├──────────────────────────────┤
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 🔧 Blutter 引擎    ││
│  │  ──────────────────────  ││
│  │  Dart 版本: v3.12.2      ││
│  │  目标: libblutter_v3.12.2││
│  │  大小: ~10MB              ││
│  │                          ││
│  │  ─────────────────████  ││
│  │  6.4 MB / 10.2 MB        ││
│  │  ⬇ 2.1 MB/s             ││
│  │  ⏱ 预计剩余: 2秒         ││
│  │                          ││
│  │  OutlinedButton: [取消]   ││
│  └──────────────────────────┘│
│                              │
│  ⏱ 已用: 3秒                │
│                              │
│  (无 BottomNav - 已隐藏)     │
└──────────────────────────────┘
```

### 9.4 产物主页 `/output/:id`

```
┌──────────────────────────────┐
│  ← [AppBar] 示例App 输出     │
├──────────────────────────────┤
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 📊 分析摘要        ││
│  │  Dart 3.12.2              ││
│  │  时长: 12.3秒             ││
│  │  ┌─────┬──────┬─────┬──┐││
│  │  │ 284 │ 1203 │  42 │4 │││
│  │  │ 类   │ 函数  │ Stub│库│││
│  │  └─────┴──────┴─────┴──┘││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 📄 pp.txt 1,892条 ││
│  │  String:423 Type:156    ││
│  │  Function:289 ...       ││
│  │  [浏览全部 ▶]            ││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 📄 objs.txt 512   ││
│  │  MaterialApp:12          ││
│  │  SizedBox:8              ││
│  │  [浏览全部 ▶]            ││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │  Card: 📁 asm/ 256个文件 ││
│  │  📂 package:bitmanager   ││
│  │  📂 package:flutter        ││
│  │  [浏览全部 ▶]            ││
│  └──────────────────────────┘│
│                              │
└──────────────────────────────┘
```

### 9.5 SO 编辑器 `/so/:id/:sofile`

```
┌──────────────────────────────┐
│  ← [AppBar] libapp.so  [↕]  │  ← 下拉切换 SO
├──────────────────────────────┤
│  TabRow: [📋结构][🔍Hex][📋反汇编]│
├──────────────────────────────┤
│                              │
│  (Tab 内容区)                 │
│                              │
│  选中地址时弹出 ContextPanel: │
│  ┌──────────────────────────┐│
│  │  InfoCard: 地址 0x1240   ││
│  │  ─────────────────────── ││
│  │  🔹 函数: main()         ││
│  │  🔹 类: ::               ││
│  │  🔹 ELF 节: .text        ││
│  │  ─────────────────────── ││
│  │  🔹 asm: BL _join        ││
│  │     → String              ││
│  │  🔹 pp: 0x0090            ││
│  │     SizedBox (Type)       ││
│  │  🔹 objs: Instance        ││
│  │     of _StringBase        ││
│  │  ─────────────────────── ││
│  │  [✏️ 编辑此地址]           ││
│  └──────────────────────────┘│
│                              │
│  底部编辑栏 (有修改时显示):   │
│  [↩ 撤销] [↪ 重做] [💾 备份] │
│  97 00 00 14 [NOP] [RET]    │
│  [应用补丁] CRC: ✅          │
│                              │
└──────────────────────────────┘
```

**结构 Tab：** ELF 头卡片 + 节头表格（名称/类型/偏移/大小）+ 符号表卡片（树形）+ 搜索栏

**Hex Tab：** 传统 Hex 布局 `偏移 | 00 01 02... | ASCII` + 反汇编小字注释 + 底部编辑栏 + 撤销栈

**反汇编 Tab：** 三列 `地址 | 字节码 | 指令+注释` + 函数标签切换 + 选中指令弹出编辑 + 搜索地址

### 9.6 指令编辑 BottomSheet

```
┌──────────────────────────────┐
│  ✏️ 编辑指令                 │
├──────────────────────────────┤
│                              │
│  地址: 0x1240                │
│  原始: 97 FF FF DC           │
│        BL _join              │
│                              │
│  ┌──────────────────────────┐│
│  │ 快捷指令                   ││
│  │  [NOP] [RET] [B]         ││
│  │  [BR x8] [自定义]         ││
│  └──────────────────────────┘│
│                              │
│  自定义:                     │
│  指令: [BL      ▼]           │
│  目标: [_join   ▼]           │
│  或输入: [0x______]          │
│                              │
│  编码预览: 97 00 00 14       │
│                              │
│  CRC 校验: ✅ 通过            │
│                              │
│  TextButton: [取消]           │
│  FilledButton: [✅ 应用补丁]  │
└──────────────────────────────┘
```

---

## 十、ARM64 编码器设计

### 10.1 支持的指令（v1.0）

| 类别 | 指令 |
|------|------|
| 加载/存储 | LDR (imm/reg/lit), LDRB, LDRH, LDRSW, STR, STRB, STRH, LDP, STP |
| 整数运算 | ADD, ADDS, SUB, SUBS, CMP, CMN, AND, ORR, EOR, BIC, ORN, MVN |
| 移位/扩展 | LSL, LSR, ASR, ROR, SXTB, SXTH, SXTW, UXTB, UXTH |
| 乘除 | MUL, SMULL, UMULL, SDIV, UDIV |
| 移动 | MOV, MOVZ, MOVN, MOVK |
| 地址 | ADRP, ADR |
| 分支 | B, BL, BLR, BR, RET, CBZ, CBNZ, TBZ, TBNZ |
| 条件选择 | CSEL, CSINC, CSINV, CSNEG, CSET, CSETM, CCMP, CCMN |
| 系统 | NOP, HLT |

### 10.2 可扩展架构

```cpp
// instruction_registry.h
class InstructionRegistry {
public:
    using EncoderFn = bool (*)(const char* args, uint32_t& encoding);

    static InstructionRegistry& instance();

    bool registerInstruction(const char* name, EncoderFn fn);
    bool encode(const char* name, const char* args, uint32_t& encoding);
    std::vector<std::string> listInstructions() const;
};

// 使用示例
REGISTER_INSTRUCTION("ADD", encodeAdd);
REGISTER_INSTRUCTION("SUB", encodeSub);
REGISTER_INSTRUCTION("DUP", encodeDup);  // v1.1 扩展 SIMD
```

---

## 十一、Scoped Storage / SAF 方案

### 11.1 文件访问策略

```kotlin
class ProjectFileHandler @Inject constructor(
    private val context: Context,
) {
    /** 使用 SAF 选择 APK 文件 */
    fun launchFilePicker(activity: Activity) {
        val intent = Intent(Intent.ACTION_OPEN_DOCUMENT).apply {
            addCategory(Intent.CATEGORY_OPENABLE)
            type = "application/vnd.android.package-archive"
            // 支持多选
            putExtra(Intent.EXTRA_ALLOW_MULTIPLE, false)
        }
        activity.startActivityForResult(intent, REQUEST_CODE_APK_PICK)
    }
    
    /** 处理 SAF 返回的 URI */
    fun handleApkSelected(uri: Uri): ApkInfo? {
        // 1. 获取持久化权限
        val flags = Intent.FLAG_GRANT_READ_URI_PERMISSION
        context.contentResolver.takePersistableUriPermission(uri, flags)
        
        // 2. 通过 ContentResolver 读取文件
        context.contentResolver.openInputStream(uri)?.use { input ->
            // 解析 APK: 提取 AndroidManifest.xml、lib/ 目录
            return parseApk(input)
        }
        return null
    }
    
    /** 从 URI 获取文件信息 */
    fun getFileInfo(uri: Uri): FileInfo {
        val cursor = context.contentResolver.query(
            uri, arrayOf(OpenableColumns.DISPLAY_NAME, OpenableColumns.SIZE),
            null, null, null
        )
        // ...
    }
}
```

### 11.2 权限策略

| Android 版本 | 策略 |
|-------------|------|
| Android 10 (API 29) | `requestLegacyExternalStorage` + SAF 双模式 |
| Android 11+ (API 30+) | 强制 SAF，`READ_MEDIA` 权限用于媒体文件 |
| Android 13+ (API 33+) | 仅 `READ_MEDIA` 细分权限（图片/视频/音频） |

### 11.3 URI 持久化

```kotlin
// 保存 URI 到项目记录
@Entity
data class ProjectEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,
    val apkUri: String,          // content:// URI
    val apkFilePath: String?,    // 兼容旧版本的文件路径
    val dartVersion: String,
    val architecture: String,
    val createdAt: Long,
)

// 从 URI 读取文件时检查权限
fun canAccessUri(uriString: String): Boolean {
    val uri = Uri.parse(uriString)
    return context.contentResolver.persistedUriPermissions
        .any { it.uri == uri && it.isReadPermission }
}
```

---

## 十二、原生库设计

### 12.1 引擎包内共享库（动态链接）

| 库 | 体积 | 说明 | 链接方式 |
|----|------|------|----------|
| `libc++_shared.so` | ~1.5MB | NDK C++ 标准库 | 所有引擎 so 动态链接 |
| `libcapstone_arm64-v8a.so` | ~2MB | ARM64 反汇编 (Capstone) | 所有引擎 so 动态链接 |
| `libicuuc.so` | ~3MB | ICU Unicode 功能库（DartVM 依赖） | 所有引擎 so 动态链接 |
| `libicudata.so` | ~5MB | ICU 数据表（DartVM 依赖） | 所有引擎 so 动态链接 |

> 以上 4 个库在引擎包内仅存 1 份，所有 Dart 版本的引擎共享。

### 12.2 按版本独立编译的引擎 so

| 库 | 体积 | 说明 | 链接方式 |
|----|------|------|----------|
| `dartvm_3.12.2_android_arm64.so` | ~2.2MB | Blutter + DartVM 3.12.2 解析 | 动态链接共享库 |
| `dartvm_3.12.1_android_arm64.so` | ~2.2MB | Blutter + DartVM 3.12.1 解析 | 动态链接共享库 |
| `dartvm_3.11.5_android_arm64.so` | ~2.2MB | Blutter + DartVM 3.11.5 解析 | 动态链接共享库 |
| `dartvm_3.10.7_android_arm64.so` | ~2.2MB | Blutter + DartVM 3.10.7 解析 | 动态链接共享库 |
| `dartvm_3.9.2_android_arm64.so` | ~2.2MB | Blutter + DartVM 3.9.2 解析 | 动态链接共享库 |
| `dartvm_3.8.1_android_arm64.so` | ~2.2MB | Blutter + DartVM 3.8.1 解析 | 动态链接共享库 |

> 每个版本的 so 静态链接该版本的 DartVM 解析逻辑（不可共享），动态链接 Capstone/ICU/c++_shared（可共享）。

### 12.3 APK 预打包库

| 库 | 体积 | 说明 | 编译方式 |
|----|------|------|----------|
| `libarm64_encoder.so` | ~200KB | ARM64 指令编码器（自研） | 预编译，打包进 APK |
| `libelf_parser.so` | ~400KB | 自研 ELF 解析器（替代 LIEF） | 预编译，打包进 APK |

> 这两个库与 Dart 版本无关，直接打包进 APK，无需远程下载。

### 12.4 体积对比

| 指标 | 静态链接（v2 方案） | 动态链接（v3 方案） | 优化幅度 |
|------|-------------------|-------------------|---------|
| 单版本引擎 | ~15MB | ~2.2MB | **-85%** |
| 6 版本原始总量 | ~90MB | ~26MB | **-71%** |
| 6 版本 7z 压缩 | ~55MB | ~7-10MB | **-82%** |
| 12 版本原始总量 | ~180MB | ~38MB | **-79%** |
| 12 版本 7z 压缩 | ~110MB | ~10-14MB | **-87%** |
| 总下载量（首次） | ~15MB/版本 | ~10-14MB（一次性） | **-90%+** |

### 12.5 CMake 动态链接改造

```cmake
# dartvm/CMakeLists.txt 关键配置
cmake_minimum_required(VERSION 3.22)
project(dartvm LANGUAGES CXX C)

set(CMAKE_CXX_STANDARD 17)

# ── 动态链接 Capstone ──
find_library(CAPSTONE_LIB capstone
    PATHS "${CMAKE_SOURCE_DIR}/../third_party/capstone/build"
    NO_DEFAULT_PATH
)

# ── 动态链接 ICU（NDK 自带）──
find_library(ICU_UC_LIB icuuc)
find_library(ICU_DATA_LIB icudata)

add_library(dartvm SHARED
    src/blutter_api.cpp
    src/blutter_entry.cpp
    # ... Blutter 源文件列表
)

# DartVM 保持静态链接（版本相关）
target_link_libraries(dartvm PRIVATE
    "${DARTVM_STATIC_LIB}"       # 必须静态
    "${CAPSTONE_LIB}"           # ✅ 动态链接
    "${ICU_UC_LIB}"             # ✅ 动态链接
    "${ICU_DATA_LIB}"           # ✅ 动态链接
    atomic log z
)

# 设置 rpath，让 blutter_*.so 能找到同目录的共享库
set_target_properties(dartvm PROPERTIES
    BUILD_SHARED_LIBS ON
    LINK_FLAGS "-Wl,-rpath,\$ORIGIN"
    INSTALL_RPATH "\$ORIGIN"
)

target_compile_options(dartvm PRIVATE -fPIC -fvisibility=hidden -O3)
target_include_directories(dartvm PUBLIC include)
```

### 12.6 JNI 桥接层

```
Kotlin Bindings (JNI)
    │
    ├── ElfParserBindings.kt → elf_parser_jni.cpp → libelf_parser.so (APK 内置)
    ├── Arm64EncoderBindings.kt → arm64_encoder_jni.cpp → libarm64_encoder.so (APK 内置)
    ├── EngineLoader.kt → 动态加载 → libc++_shared.so + libicuuc.so + libcapstone.so (引擎包共享)
    └── BlutterEngine.kt → blutter_jni.cpp → dartvm_{version}.so (按需加载)
```

---

## 十三、完整工作流程

### 13.1 首次使用

```
① 安装 fler → 启动
      │
      ▼
② 首次检测: 本地无引擎包？
      ├── 是 → 显示引擎包下载页
      │       ├── 显示: fler-engines.7z ~10-14MB
      │       ├── Gitee 优先 / GitHub 回退
      │       ├── 前台服务下载 + 进度通知
      │       ├── SHA256 校验 + 7z 解压
      │       └── 解压完成 → 引擎就绪
      └── 否 → 跳过下载，直接使用
      │
      ▼
③ 首次使用 → 展示新手引导 (/tutorial)
      │
      ▼
④ 项目列表 → [+ 新建项目]
      │
      ▼
⑤ SAF 选择 APK → 自动检测 Dart 版本 + 架构
      │ 非 arm64-v8a 时提示 "仅支持 arm64-v8a"
      │ Dart 版本不在兼容列表时提示 "版本 v3.x.y 尚未编译"
      ▼
⑥ 项目详情 → [▶ 运行 Blutter]
      │
      ▼
⑦ 引擎加载（零下载，引擎包已就位）
   → System.load 按依赖链加载共享库 + 引擎 so
   → nativeCreate + nativeBlutterAnalyze
   → 直接写入 SQLite 数据库
      │
      ▼
⑧ 自动构建地址映射表 → 存入 Room
      │
      ▼
⑨ 产物主页 → 浏览 pp_entries / classes / methods
      │
      ▼
⑩ SO 编辑器 → ELF 结构查看 / Hex 编辑 / 反汇编
      │
      ▼
⑪ 产物 ↔ SO 联动:
   - pp 条目 [SO中定位] → 自动跳转 Hex
   - asm 函数 [SO中定位] → 跳转反汇编 Tab
```

### 13.2 二次使用（同版本或不同版本）

```
① 打开项目 → [▶ 运行 Blutter]
② 引擎包 ✅ 已就位 ✅
   → 按依赖链加载共享库（进程内只加载一次）
   → 加载目标 Dart 版本的引擎 so
   → nativeBlutterAnalyze 直接写 SQLite
   → 运行分析 (10-30秒)
③ 浏览 / 编辑 / 导出
```

### 13.3 引擎包升级

```
① App 启动 → checkForUpdates()
② 发现新版本 → 显示更新提示
③ 用户确认 → 后台下载新版本 7z
④ 下载完成 → 覆盖解压 → 旧版本自动清理
⑤ 后续分析使用新版本引擎
```

### 13.4 产物 ↔ SO 联动

```
📦 产物浏览器                    🔧 SO 编辑器
─────────────────────────────────────────────────
pp_entries 条目 [SO中定位] ───▶ AddressTranslator
                              → vmOffsetToFileOffset()
                              → Hex 定位 + ContextPanel
classes 实例 [SO中定位] ─────▶ AddressTranslator
                              → vmOffsetToFileOffset()
                              → Hex 定位 + ContextPanel
methods 函数 [SO中定位] ─────▶ AddressTranslator
                              → findFunctionByAddress()
                              → 反汇编 Tab 定位函数
```

---

## 十四、技术选型

| 层次 | 技术 | 原因 |
|------|------|------|
| UI 框架 | Jetpack Compose + Material Design 3 | 原生 Android，Material 3 卡片式 UI |
| 状态管理 | Kotlin StateFlow + ViewModel | 响应式，生命周期感知 |
| DI | Hilt | 官方推荐，编译期安全 |
| 本地数据库 | Room | 持久化映射表 + 撤销记录 |
| 导航 | Navigation Compose | 类型安全路由 + 动画 |
| 网络 | OkHttp + Retrofit | 代理支持 + 断点续传 |
| 文件访问 | SAF + ContentResolver | Scoped Storage 兼容 |
| 反汇编 | Capstone (ARM64 only) 动态链接 | 成熟稳定，引擎包共享 |
| ARM64 编码 | 自研轻量编码器 | 50 条指令 + 插件注册 |
| ELF 解析 | 自研轻量解析器 | <500KB，零依赖 |
| 异步 | Kotlin Coroutines + Flow | 流式状态管理 |
| 日志 | Timber | 轻量日志 |
| 前台服务 | Foreground Service | Android 14+ 长任务合规 |
| 压缩 | 7z (LZMA2) | 高压缩率，引擎包 ~10-14MB |
| 引擎分发 | 单包 7z + 动态链接 | 首次下载零等待 |
| 下载源 | Gitee + GitHub 双源 | 国内加速 25 倍 |
| 测试 | JUnit5 + MockK + Compose Test | 全层级覆盖 |
| CI | GitHub Actions + Gitee 镜像 | 引擎包自动构建 + 发布 |

---

## 十五、内存管理策略

| 策略 | 实现方式 | 适用场景 |
|------|----------|----------|
| 懒加载 | Room 分页查询 + Paging 3 | pp/objs 列表 |
| 虚拟滚动 | LazyColumn + 固定 item 高度 | 大列表页面 |
| 流式解析 | kotlinx-io 流式读取，逐行解析 + 写入 Room | pp.txt/objs.txt/asm 解析 |
| 分页 | 按偏移范围分段加载 | Hex 编辑器大文件 |
| 按需释放 | Compose 重组自动回收不可见项 | asm 文件树 |
| Native 层缓存 | 限制 Blutter 分析结果内存上限 | 大型 APK 分析 |
| 图片缓存 | Coil + 磁盘缓存 | 缩略图/图标 |

---

## 十六、实施阶段

| 阶段 | 周次 | 交付物 |
|------|------|--------|
| **P0 技术验证** | 1 | CMake 动态链接改造验证（单版本 so 从 15MB 降至 ~2MB）+ ICU 依赖验证 + 自研 ELF 解析器原型 + AddressTranslator 算法验证 + ARM64 编码器原型 |
| **P1 骨架** | 2-3 | Kotlin 工程 + Hilt 注入 + Navigation Compose 路由 + 4 Tab 导航 + Material 3 主题 + 通用组件库 + 预置演示数据 |
| **P2 原生库** | 4-5 | capstone/arm64_encoder/elf_parser 三库 NDK 编译脚本 + JNI 桥接 + CI 动态链接构建 + 可扩展指令注册架构 |
| **P3 引擎包** | 6-7 | EnginePackManager + 7z 解压 + 动态链接加载器 + Gitee/GitHub 双源下载 + 前台服务 + 引擎管理页 |
| **P4 产物解析** | 8-9 | BlutterEngine JNI 封装 + SQLite 解析 + Room 实体/DAO + AddressTranslator 实现 + 产物浏览全部页面 + 懒加载 |
| **P5 SO 编辑器** | 10-11 | BackupManager(Room 持久化) + 结构 Tab + Hex Tab + 反汇编 Tab + ContextPanel + EditInstructionSheet + 撤销栈 + 产物↔SO 联动 |
| **P6 集成收尾** | 12 | 引擎包自动构建 CI + Gitee 镜像 + 版本更新检测 + 新手引导 + 导出功能 + 完整流程联调 + 性能优化 + 测试覆盖 |

---

## 十七、测试策略

| 层级 | 工具 | 覆盖范围 |
|------|------|----------|
| **单元测试** | JUnit5 + MockK | 所有 Parser、Translator、BackupManager、EngineDownloader、ElfParser |
| **ViewModel 测试** | JUnit5 + Turbine | 所有 ViewModel 的状态机逻辑 |
| **UI 测试** | Compose UI Test | 关键页面的交互路径 |
| **集成测试** | AndroidX Test | 引擎下载流程 + SO 编辑流程 + 产物联动 |
| **Native 测试** | GTest | ELF 解析器 + ARM64 编码器 |
| **CI** | GitHub Actions | PR 自动运行单元测试 + 自动构建原生库 |

---

## 十八、关键设计原则

| 原则 | 说明 |
|------|------|
| **单包分发，零等待** | 首次下载 `fler-engines.7z`（~10-14MB），包含所有 Dart 版本 + 共享依赖，后续零下载 |
| **动态链接省空间** | Capstone/ICU/c++_shared 动态链接，单版本引擎从 ~15MB 降至 ~2.2MB |
| **远程编译，本地分析** | GitHub Actions 预编译 Blutter 引擎，移动端仅下载运行 |
| **原生优先** | Kotlin + Compose + Material 3，充分利用 Android 平台能力 |
| **自研轻量** | ELF 解析器、ARM64 编码器全部自研，体积最小、无外部依赖 |
| **卡片式 UI** | 所有内容区块均为 Material 3 Card |
| **移动端优先** | 单列垂直布局，底部导航仅顶层可见 |
| **4 Tab 导航** | 项目 / 产物 / SO / 设置 |
| **国内加速** | Gitee 镜像优先 + GitHub 回退，国内下载提速 25 倍 |
| **产物即为上下文** | pp/objs/asm 融合进 SO 编辑器的上下文卡片 |
| **交叉引用** | 产物页 [SO中定位] → AddressTranslator → 自动跳转 |
| **安全编辑** | 自动备份 + Room 持久化撤销 + CRC 校验 + 失败回滚 |
| **数据持久化** | Room 缓存分析结果 + 地址映射 + 撤销历史 |
| **内存安全** | 懒加载 + 虚拟滚动 + 流式解析 |
| **可扩展编码器** | ARM64 指令通过注册表模式扩展 |
| **Scoped Storage** | 全面支持 SAF，兼容 Android 11+ |
| **SQLite 直写** | Blutter 直接写入 SQLite 数据库，省去文件解析步骤 |

---

## 十九、GitHub Actions CI 设计

### 19.1 引擎包自动构建（单包分发）

```yaml
# .github/workflows/engine-package-build.yml
name: Build Engine Package

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      dart_versions:
        description: 'Dart SDK versions (comma-separated)'
        required: false
        default: '3.12.2,3.12.1,3.11.5,3.10.7,3.9.2,3.8.1'

jobs:
  # ── Step 1: 构建共享依赖（仅一次）──
  build-shared:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup NDK
        uses: android-actions/setup-android@v3
        with:
          ndk-version: '27.0.12077973'

      - name: Build shared libraries
        run: |
          mkdir -p output/shared/
          
          # libc++_shared.so (NDK 自带)
          cp $ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib/aarch64-linux-android/libc++_shared.so output/shared/
          
          # libcapstone.so (动态库)
          cd third_party/capstone
          cmake -B build -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_HOME/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a -DANDROID_PLATFORM=android-24 -DBUILD_SHARED_LIBS=ON
          ninja -C build
          cp build/libcapstone.so output/shared/libcapstone_arm64-v8a.so
          cd ../..
          
          # ICU 库 (NDK 自带)
          cp $ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib/aarch64-linux-android/libicuuc.so output/shared/
          cp $ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/sysroot/usr/lib/aarch64-linux-android/libicudata.so output/shared/

      - name: Upload shared artifacts
        uses: actions/upload-artifact@v4
        with:
          name: shared-libs
          path: output/shared/

  # ── Step 2: 构建各版本引擎（矩阵并行）──
  build-engines:
    needs: build-shared
    strategy:
      matrix:
        dart_version: ['3.12.2', '3.12.1', '3.11.5', '3.10.7', '3.9.2', '3.8.1']
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup NDK
        uses: android-actions/setup-android@v3
        with:
          ndk-version: '27.0.12077973'

      - name: Build dartvm for Dart ${{ matrix.dart_version }}
        run: |
          bash scripts/build-dartvm.sh ${{ matrix.dart_version }}
          cp output/dartvm_${{ matrix.dart_version }}_android_arm64.so output/

      - name: Upload engine artifact
        uses: actions/upload-artifact@v4
        with:
          name: engine-${{ matrix.dart_version }}
          path: output/dartvm_${{ matrix.dart_version }}_android_arm64.so

  # ── Step 3: 打包成单个 7z 包 ──
  package:
    needs: [build-shared, build-engines]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Create 7z package
        run: |
          mkdir -p final/shared/
          
          # 拷贝共享库
          cp artifacts/shared-libs/libc++_shared.so final/shared/
          cp artifacts/shared-libs/libcapstone_arm64-v8a.so final/shared/
          cp artifacts/shared-libs/libicuuc.so final/shared/
          cp artifacts/shared-libs/libicudata.so final/shared/
          
          # 拷贝各版本引擎
          for version in 3.12.2 3.12.1 3.11.5 3.10.7 3.9.2 3.8.1; do
            cp artifacts/engine-${version}/dartvm_${version}_android_arm64.so final/
          done
          
          # 计算 SHA256
          cd final
          find . -name '*.so' -exec sha256sum {} \; > checksums.txt
          cat checksums.txt
          cd ..
          
          # 压缩成 7z
          7z a -mx=9 -mhe=on fler-engines.7z final/
          ls -lh fler-engines.7z

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          assets: |
            fler-engines.7z
            final/checksums.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

### 19.2 build-dartvm.sh 关键参数（动态链接）

```bash
# scripts/build-dartvm.sh (关键参数)
cmake -B build -G Ninja \
  -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_HOME/build/cmake/android.toolchain.cmake \
  -DANDROID_ABI=arm64-v8a \
  -DANDROID_PLATFORM=android-24 \
  -DANDROID_STL=c++_shared \          # ✅ 使用 c++_shared（动态链接）
  -DBLUTTER_DART_VERSION=$1 \
  -DCMAKE_BUILD_TYPE=Release \
  -DBUILD_SHARED_LIBS=ON \
  # ... 其他参数
```

### 19.3 Gitee 镜像配置

```yaml
# 在 Gitee 创建镜像仓库，配置自动同步
# Webhook: GitHub → Gitee 自动同步 Release Assets
# App 默认从 Gitee 下载，失败时回退到 GitHub
```

### 19.4 Release 流程

```
1. 更新 blutter 源码或 Dart SDK 版本
2. 推送 tag: git tag v1.0.0 && git push origin v1.0.0
3. GitHub Actions 自动触发:
   a. 构建共享库（libc++_shared, libcapstone, libicuuc, libicudata）
   b. 并行构建 12 个 Dart 版本引擎（动态链接共享库）
   c. 计算 SHA256 校验
   d. 打包成 fler-engines.7z（~10-14MB）
   e. 上传到 GitHub Release
4. Gitee 镜像自动同步
5. fler App 端:
   a. 首次启动显示下载页
   b. 优先从 Gitee 下载 fler-engines.7z（国内加速）
   c. SHA256 校验 + 7z 解压
   d. 后续分析任意版本零下载
```

### 19.5 CI 流水线扩展

```yaml
# PR 触发的检查
pr-checks:
  jobs:
    unit-tests:
      run: ./gradlew testDebugUnitTest
    lint:
      run: ./gradlew lintDebug
    native-tests:
      run: cd native && cmake -B build && ninja -C build && ctest -C build
```

---

## 二十、风险与缓解

| 风险 | 影响 | 缓解方案 | 状态 |
|------|------|----------|------|
| 引擎包下载失败 | 无法分析 | Gitee/GitHub 双源 + 断点续传 + 前台服务 | 已缓解 |
| 共享库加载顺序错误 | `UnsatisfiedLinkError` | 严格按依赖链顺序加载（c++_shared → ICU → Capstone → 引擎） | 需验证 |
| Dart 版本不在兼容列表 | 无法分析 | 提示用户提 Issue + 显示已支持版本列表 | 已缓解 |
| 动态链接找不到共享库 | 加载失败 | rpath 设置 `$ORIGIN` + 加载前校验文件完整性 | 需验证 |
| 自研 ELF 解析器功能不足 | SO 编辑受限 | 预留 LIEF 集成备选方案 | 需验证 |
| SAF URI 权限丢失 | 无法访问 APK | takePersistableUriPermission + 权限检查 | 已缓解 |
| 分析文件过大 OOM | 应用崩溃 | 流式解析 + Room 分页 + 懒加载 | 已缓解 |
| 引擎包下载时被系统杀死 | 下载中断 | Foreground Service + 通知 + 断点续传 | 已缓解 |
| Blutter 输出格式变化 | 解析失败 | 版本化 Parser + 容错解析 + 错误日志 | 需验证 |
| ICU 库版本不匹配 | 引擎崩溃 | CI 固定 NDK 版本 + ICU 库随引擎包分发 | 需验证 |
| 7z 解压失败（文件损坏） | 引擎不可用 | 下载后 SHA256 校验 + 解压重试机制 | 需验证 |
| 动态链接在某些 ROM 不兼容 | 引擎加载失败 | 保留静态链接版本作为降级方案 | 需验证 |

---

## 附录 A：术语表

| 术语 | 说明 |
|------|------|
| Dart VM | Dart 语言的运行时虚拟机 |
| pp.txt | Blutter 输出的预分配对象池文件 |
| objs.txt | Blutter 输出的对象实例文件 |
| asm/ | Blutter 输出的 ARM64 反汇编文件目录 |
| ELF | Executable and Linkable Format，可执行文件格式 |
| SO | Shared Object，ELF 动态库文件 |
| SAF | Storage Access Framework，Android 文件访问框架 |
| W^X | Write XOR Execute，安全策略 |
| mmap | Memory Map，内存映射文件访问 |
| CRC32 | Cyclic Redundancy Check，循环冗余校验 |

## 附录 B：v2 → v3 变更总结

| 模块 | v2 | v3 |
|------|----|----|
| **引擎分发** | 按 Dart 版本独立下载单版本 .so | **单包 7z 分发**（fler-engines.7z，包含所有版本） |
| **链接方式** | 静态链接（每个 so 含 Capstone/ICU/c++_static） | **动态链接**（共享库只存 1 份，单版本引擎 ~2.2MB） |
| **引擎大小** | ~15MB/版本 × 12 = 180MB | ~2.2MB/版本 × 12 + 11MB 共享 = 37MB |
| **压缩后大小** | ~110MB | **~10-14MB** |
| **下载方式** | 按需下载（每次换版本需重下） | **首次下载一次**，后续零等待 |
| **下载源** | GitHub 仅 | **Gitee + GitHub 双源**（国内加速 25 倍） |
| **分析输出** | pp.txt/objs.txt/asm 文件 | **直接写 SQLite 数据库**（省去文件解析步骤） |
| **Capstone** | 静态链接，12 份重复 | 动态链接，引擎包内 1 份共享 |
| **ICU** | 未考虑（隐式依赖） | **明确加入共享依赖**（DartVM Unicode 处理必需） |
| **CMake** | 默认静态链接 | 改为 `c++_shared` + `find_library` 动态链接 |
| **EngineDownloader** | 按版本下载 | **EnginePackManager** 单包下载 + 解压 + 版本管理 |
| **BlutterBindings** | 纯 JNI 加载 | **EngineLoader** + 依赖链顺序加载 |
| **新增章节** | - | 4.2 共享依赖 / 4.3 引擎包结构 / 4.4 动态链接说明 / 4.5 加载顺序 / 4.6 下载源 / 5.3 7z 解压 / 5.7 双源策略 / 12.4 体积对比 / 12.5 CMake 改造 |