构建前需确保 `libs/arm64-v8a/libkeystone.a` 存在（由 `build.gradle.kts` 中的 `fetchKeystone` 任务校验），该静态库需通过本地交叉编译脚本 `scripts/build-keystone.sh` 或 GitHub Actions workflow 产物放置；Rizin 与 Capstone 的 `.a` 静态库同样来自外部 workflow 产物，直接由 CMake `IMPORTED` 目标引用。