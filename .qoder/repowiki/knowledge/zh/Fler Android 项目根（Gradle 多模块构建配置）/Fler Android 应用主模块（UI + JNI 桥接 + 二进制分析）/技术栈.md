Android Gradle (Kotlin DSL) + Jetpack Compose + Material3 + Navigation Compose；依赖注入使用 Hilt（ksp 编译期生成）；持久化使用 Room（ksp 注解处理器）；网络 OkHttp；JSON 序列化 kotlinx-serialization；原生层 C++20 + CMake 3.22，静态链接 Capstone、Keystone、Rizin（librz_*）与 sdb，NDK 仅 arm64-v8a ABI，STL 使用 c++_shared。