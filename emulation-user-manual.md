# fler 仿真引擎用户手册

fler 内置基于 **Unicorn 2.0.1（ARM64）** 的指令级仿真引擎：可以在不真机执行的情况下，
直接调用 .so 里的函数、单步跟踪、下断点、读写寄存器和内存。适用于算法还原、
校验函数爆破、加密例程行为观察等逆向场景。

> 演示样本：仓库 `test-samples/libemu_demo.so`（源码 `emu_demo.c`），本文所有示例都以它为对象。

---

## 1. 准备工作

- APK 必须是 **debug 构建**（fler_jni 含 Unicorn 静态库，release 同样可用）
- 目标 .so 必须是 **arm64-v8a**（当前仅支持该架构）
- 把 `libemu_demo.so` 放到手机存储（如 `Download/`），或直接通过 APK 导入流程提取

## 2. 打开 .so 的两种入口

| 入口 | 路径 |
|---|---|
| SO 编辑器（独立） | 底部「SO 编辑器」Tab → 点文件选择按钮 → 选中 `libemu_demo.so` |
| 项目产物 | 导入 APK → 运行分析 → 项目详情「SO 文件」列表 → 点击某个 .so |

打开后在顶部 Tab 栏切到 **「仿真」**。首次进入会自动打开仿真会话
（映射 PT_LOAD 段 + 栈 + heap，通常 <1 秒）。

## 3. 仿真 Tab 界面说明

自上而下四个卡片：

1. **函数调用**：函数名输入框（支持模糊下拉 + 直接输地址 `0x…`）+ x0-x7 参数格（hex/十进制均可）+「调用」
2. **执行控制**：运行 / 单步 / 停止 + 断点添加 + 状态 chips（已执行条数、停止原因、PC、断点数）
3. **调用结果 / 寄存器**：返回值卡片（无符号 + 有符号 + 停止原因 + 指令数）；全寄存器网格**点击可编辑**
4. **日志**：最近 200 条操作记录，可清空

## 4. 快速上手：调用 demo 函数

打开仿真 Tab 后逐个试：

| 函数 | 参数 | 预期返回 | 看点 |
|---|---|---|---|
| `add` | x0=`0x1234` x1=`0x5678` | `0x68ac` | 最小闭环：参数透传 + 返回值 |
| `abs64` | x0=`-5` | `5` | 负数十进制输入 |
| `sum_to` | x0=`100` | `4950`（0x1356） | 循环执行 |
| `fib` | x0=`25` | `75025`（0x12511），约 250 万条指令 | 递归 + 栈使用 + 指令计数 |
| `reverse_bytes` | x0=`0x1122334455667788` | `0x8877665544332211` | 64 位位操作 |
| `spin` | x0=`1000000` | `499999500000`（0x746A4AE6E0），约 200 万条指令 | 观察执行耗时（~0.4s） |

每次调用成功后，结果卡片会显示 `x0` 值、停止原因（应为「函数返回」）和指令数；
寄存器区也会刷新（pc 停在哨兵地址 `0xdeadbeef0000`）。

## 5. 断点与单步工作流

以 `sum_to` 为例观察循环内部：

1. 先在反汇编 Tab 找到 `sum_to`（地址 `0x4874`）内循环体的某条指令地址，
   比如 `0x4880`
2. 回到仿真 Tab，「断点地址」输入 `0x4880` → 添加（出现蓝色 chip）
3. 函数名输 `sum_to`、x0=`10` →「调用」：执行会在断点处停下，
   停止原因显示「断点」（蓝色 chip），pc = 断点地址
4. 反复点「单步」逐条前进，观察 x0/x1 等寄存器逐条变化
5. 点「运行」继续执行直到下一个断点或函数返回
6. 不想要了：点断点 chip 上的 ×，或点「停止」中断执行

> 断点语义：停在**尚未执行**的断点指令上（经典调试器语义）。

## 6. 编辑寄存器

点击寄存器网格中任意 chip（如 `x0`）→ 弹窗输入新值（`0x…` 或十进制）→ 写入。
配合断点可实现：断下 → 改 x0 → 运行，观察分支变化。
改 `pc` 可以跳转执行流（高级玩法，注意别跳进数据段）。

## 7. 仿真内存布局

打开会话时的固定地址空间：

| 区域 | 地址范围 | 权限 | 用途 |
|---|---|---|---|
| so 的 PT_LOAD 段 | 按 ELF 声明（demo 库约 `0x0~0x6xxx`） | RX/RW | 代码与数据 |
| 栈 | `0x40000000 ~ 0x40100000`（1MB） | RW | SP 初始指向栈顶 |
| 简易 heap | `0x50000000 ~ 0x50800000`（8MB） | RW | 传指针参数用它当缓冲区 |
| 哨兵页 | `0xDEADBEEF0000` | RX | LR 指向它，执行到此 = 函数返回 |

**需要指针参数的函数**（demo 里的 `xor_buf` / `hash_buf` / `str_len`）在 UI 上
无法直接准备内存内容，推荐走 MCP（见下节），或先手动往 heap 写数据。

## 8. MCP 自动化（emu_* 工具）

在设置页开启 MCP 服务器后，AI 助手可调用 14 个 `emu_*` 工具。
完整工作流示例——**用 `hash_buf` 计算 "fler!" 的 djb2**：

```
1. emu_open          { "soPath": "/storage/emulated/0/Download/libemu_demo.so" }
2. emu_write_memory  { "soPath": "...", "address": "0x50000000", "hex": "666c65722100" }
3. emu_call_function { "soPath": "...", "function": "hash_buf", "args": ["0x50000000", "5"] }
   → 返回 x0 = 0x310F71B8EF
4. emu_read_memory   { "soPath": "...", "address": "0x50000000", "size": 6 }
   → 验证写入内容
```

`xor_buf` 加密观察：写明文 → `emu_call_function xor_buf(0x50000000, 5, 0xAA)` →
`emu_read_memory` 读回密文 → 再调一次还原。

工具全表：`emu_open` / `emu_close` / `emu_list_sessions` / `emu_call_function` /
`emu_run` / `emu_step` / `emu_stop` / `emu_read_registers` / `emu_write_register` /
`emu_read_memory` / `emu_write_memory` / `emu_map_memory` / `emu_breakpoint_add` /
`emu_breakpoint_remove` / `emu_breakpoint_list`

## 9. 限制与常见问题

| 现象 | 原因 / 对策 |
|---|---|
| 「仿真引擎不可用」 | 构建未链入 Unicorn（检查 `ENABLE_UNICORN`）；正式版始终包含 |
| 调用报「函数未找到」 | 函数名不在符号表：换导出名，或直接输 `0x` 地址 |
| 执行到某处报「错误」停止 | 访问了未映射内存（常见：调用了 libc/PLT、指针参数无效）。仿真**不解析 DT_NEEDED、不模拟 libc**，依赖外部库的函数无法直接跑 |
| 无限循环函数 | 默认 2000 万条指令 / 30s 超时兜底；也可随时点「停止」 |
| 停止原因「超时」 | 加大参数前先估算指令量（参考 `spin`：100 万次 ≈ 200 万条指令 ≈ 0.4s） |
| 仅支持 arm64 | armeabi-v7a / x86 的 so 无法仿真 |

---

*引擎版本：Unicorn 2.0.1（静态链入 fler_jni）；性能基线：~5.8 MIPS（含 hook 开销，真机实测）。*
